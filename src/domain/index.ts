/**
 * @fileoverview Domain Layer Barrel Export
 *
 * Central export point for all domain types and schemas.
 * The domain layer contains pure business logic with no external dependencies.
 *
 * @module domain
 * @since 5.0.0
 */

// Config domain
export * from "./config/index.ts";
